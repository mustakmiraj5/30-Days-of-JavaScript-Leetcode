var createHelloWorld = function() {
    
    return function(...args) {
        return "Hello World"
    }
};

/**
 * const f = createHelloWorld();
 * f(); // "Hello World"
 */

// const f = createHelloWorld();
// console.log(f());

// Editorial

/**
 * Closures and Higher order functions.
 * Basic syntax, Anonymus function, Immediately Invoked Function Expression (IIFE).
 * Functions Within Functions.
 * Function Hoisting.
 * Closures, lexical environment.
 * Omit return.
 * Rest Arguments.
 */